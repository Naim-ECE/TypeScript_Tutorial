const enum seatChoice {
  AISLE = "aisle",
  MIDDLE = 21, // numbers are auto generated and if a string is added then next value needs to be string to fulfill the order or it can also be  a number then it will add to the next by default
  WINDOW,
  FOURTH = 40,
}

const hcSeat = seatChoice.AISLE;

export {};
